const superHeroes: string[] = [];
const heroPwer: number[] = [];
const arr: Array<number> = [];

type User = {
  name: string;
  isActive: boolean;
};

const allUsers: User[] = [];

// Array of Array
const MLModels: number[][] = [
  [255, 255, 255],
  [1, 2, 3],
];

superHeroes.push("spidy");
heroPwer.push(2);

// allUsers.push("")  // Gives Error

allUsers.push({ name: "Spiderman", isActive: true });

export {};
